puntowebferrara-COMPLETO.zip
============================

VA IN:  C:\Hugo\puntowebferrara-hugo

26 file. Contiene TUTTO il lavoro di stanotte per questo sito, in una
volta sola: se hai gia' messo qualche zip precedente, questo lo
sovrascrive con la versione definitiva. Se te ne sei perso qualcuno,
questo lo recupera.

PRIMA DI SCOMPATTARE
--------------------
Se non l'hai gia' fatto, la vecchia pagina verifica va tolta dal repo:

  git rm content/verifica.md layouts/page/verifica.html
  git rm -r static/verifica/

Se rispondono "did not match any files", vuol dire che l'hai gia' fatto.

DOPO AVER SCOMPATTATO, CONTROLLA CHE SIA ENTRATO TUTTO
-------------------------------------------------------
  Select-String -Path .\hugo.toml -Pattern "Strumenti"
  Select-String -Path .\layouts\_default\baseof.html -Pattern "ha-tendina"
  Select-String -Path .\layouts\page\sottosopra.html -Pattern "modo2"

Le prime due DEVONO stampare righe. La terza NON deve stampare niente.
Poi git status deve elencare 26 file (piu' le cancellazioni).

COSA C'E' DENTRO
----------------
MENU
  hugo.toml, baseof.html
  La voce "Sottosopra" diventa "Strumenti" con quattro sottovoci.
  Aggiunto anche il link "salta al contenuto" su tutte le pagine.

I TRE STRUMENTI, UNO PER COMPITO
  /sottosopra/                 solo analisi tecnica. Niente parole chiave,
                               niente Turnstile, nessun costo.
  /sottosopra/posizionamento/  solo posizioni su Google. Turnstile qui.
  /sottosopra/accessibilita/   35 controlli WCAG 2.1 AA. NUOVA.

  Il codice del rapporto posizioni sta in un file solo
  (posizioni-blocco.js) usato da chi serve: nessuna copia doppia.

ACCESSIBILITA' DEL TUO SITO
  Etichette ai campi degli strumenti, autocomplete su contatti e
  preventivo, avviso "si apre in una nuova scheda" su tutti i link
  esterni, link di salto al contenuto.

  Misurato ricompilando: da 90 a 100 di media su 27 pagine.

DA PROVARE DOPO IL DEPLOY
-------------------------
1. Menu "Strumenti": quattro voci, e provalo anche col solo Tab.
2. /sottosopra/ analisi completa: il rapporto NON deve piu' contenere
   il blocco delle posizioni.
3. /sottosopra/posizionamento/: due analisi di fila devono funzionare
   entrambe (il token Turnstile si rigenera dopo ogni uso).
4. /sottosopra/accessibilita/ su un sito qualsiasi.
5. Premere un pulsante senza fare la verifica Turnstile: deve avvisare,
   non fallire.
